The Jupyter notebooks here are entirely refactored to work locally in Jupyter Lab + TensorFlow Datasets (TFDS) without relying on Google Cloud.
I've kept the same folder structure as used in the example files from the book, but varied from the topics as necessary (often heavily at times).
